name: common
description: A starting point for Dart libraries or applications.
version: 1.0.0

environment:
  sdk: ^3.1.0

# Add regular dependencies here.
dependencies:
  get_it: ^7.6.4
  logger: ^2.0.2+1

dev_dependencies:
  lints: ^2.0.0
  test: ^1.21.0
